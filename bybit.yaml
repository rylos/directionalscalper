session_name: bybit
windows:
- window_name: ds
  layout: tiled
  shell_command_before:
    - cd /opt/directionalscalper-rylos
  panes:
    - shell_command:
      - python3.11 multi_bot.py --exchange bybit --account_name bybit --strategy bybit_mm --config config.json
